# python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# environments
.venv/

# experiment outputs (artifacts are archived, not committed; keep dir via .gitkeep)
experiments/*
!experiments/.gitkeep

# dataset cache
*.parquet
data/cache/

# reference PDFs live in papers/ — kept local, never committed to GitHub
*.pdf

# local assistant context — kept local, not committed
AGENTS.md
